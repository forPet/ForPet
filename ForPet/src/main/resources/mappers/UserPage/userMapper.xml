<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.forpet.mapper.UserPage.userMapper">
	<!-- 로그인 -->
	<select id="login" resultType="UserVO">
		select
		*
		from
		tbl_user
		where
		userId = #{userId} and userPassword = #{userPassword}
	</select>
	<!-- 로그인 권한 부여 -->
	<select id="sitterCheck">
		select 
			userNumber
		from 
			tbl_sitter
		where 
			sitterNumber = #{sitterNumber}
	</select>
	<!-- 마이페이지 -->
	<select id="myPage" resultType="UserVO">
		select
		userId,
		userName,
		userEmail,
		userAddress
		from
		tbl_user
		where
		userId = #{userId}
	</select>
</mapper>
