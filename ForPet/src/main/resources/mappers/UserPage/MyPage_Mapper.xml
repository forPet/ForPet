<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.forpet.MyPage_Mapper">
	
	<select id="reservationSearch" resultType="BookingDTO">
	select a.bookingDate bookingDate, b.title title, b.start start, b.end end, a.progressState progressState from tbl_booking a, tbl_booking_schedule b where a.bookingNumber = b.bookingNumber and a.userNumber = #{userNumber};
	</select>
</mapper>